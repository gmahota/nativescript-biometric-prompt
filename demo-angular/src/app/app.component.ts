import { BiometricPrompt } from 'nativescript-biometric-prompt';
console.log(new BiometricPrompt().message);
import { Component } from "@angular/core";

@Component({
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
